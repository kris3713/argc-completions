#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @option --check-tmpdir* <ignore|>          Check there is enough space in $TMPDIR
# @flag --color                              Use ANSI colour sequences even if not tty
# @flag --colors                             Use ANSI colour sequences even if not tty
# @flag --colour                             Use ANSI colour sequences even if not tty
# @flag --colours                            Use ANSI colour sequences even if not tty
# @flag --compress                           Compressed output format
# @option --convert <format>                 Format of output disk (default: same as input)
# @flag --echo-keys                          Don’t turn off echo for passphrases
# @option --format <format>                  Format of input disk
# @flag --help                               Display brief help
# @option --ignore <fs>                      Ignore filesystem
# @flag --in-place                           Modify the disk image in-place
# @flag --inplace                            Modify the disk image in-place
# @option --key <SELECTOR>                   Specify a LUKS key
# @flag --keys-from-stdin                    Read passphrases from stdin
# @option --machine-readable <format>        Make output machine readable
# @option -o <option>                        Add qemu-img options
# @flag -q --quiet                           Don’t print progress messages
# @option --tmp <block|dir|prebuilt:file>    Set temporary block device, directory or prebuilt file
# @flag -V --version                         Display version and exit
# @flag -v --verbose                         Enable libguestfs debugging messages
# @flag --wrap                               Wrap log messages even if not tty
# @flag -x                                   Enable tracing of libguestfs calls
# @option --zero <fs>                        Zero filesystem

command eval "$(argc --argc-eval "$0" "$@")"