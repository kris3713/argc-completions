#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag --bun                       Force the command to run with Bun instead of Node.js
# @option -p --package <package>    Specify package to install when binary name differs from package name
# @flag --no-install                Skip installation if package is not already installed
# @flag --verbose                   Enable verbose output during installation
# @flag --silent                    Suppress output during installation
# @arg package-version <<package><@version>>

command eval "$(argc --argc-eval "$0" "$@")"