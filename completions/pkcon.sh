#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -h --help              Show help options
# @flag --help-all             Show all help options
# @flag --version              Show the program version and exit
# @flag --filter               Set the filter, e.g. installed
# @flag -y --noninteractive    Install the packages without asking for confirmation
# @flag -d --only-download     Prepare the transaction by downloading packages only
# @flag --allow-downgrade      Allow packages to be downgraded during transaction
# @flag --allow-reinstall      Allow packages to be reinstalled during transaction
# @flag --autoremove           Automatically remove unused dependencies
# @flag -n --background        Run the command using idle network bandwidth and also using less power
# @flag -p --plain             Print to screen a machine readable output, rather than using animated widgets
# @flag -c --cache-age         The maximum metadata cache age (in seconds).
# @flag --allow-untrusted      Allow untrusted packages to be installed.
# @flag -v --verbose           Show debugging information for all files
# @arg packagekit
# @arg console
# @arg program

command eval "$(argc --argc-eval "$0" "$@")"