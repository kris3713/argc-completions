#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.



# {{ corepack disable
# @cmd
# @option --install-directoryWhere the shims are located  0
disable() {
    :;
}
# }} corepack disable

# {{ corepack enable
# @cmd
# @option --install-directoryWhere the shims are to be installed  0
enable() {
    :;
}
# }} corepack enable

# {{ corepack install
# @cmd
install() {
    :;
}
# }} corepack install

# {{ corepack pack
# @cmd
# @flag --json    If true, the path to the generated tarball will be printed on stdout
# @option -o --outputWhere the tarball should be generated; by default "corepack.tgz"  0
pack() {
    :;
}
# }} corepack pack

# {{ corepack up
# @cmd
up() {
    :;
}
# }} corepack up

command eval "$(argc --argc-eval "$0" "$@")"