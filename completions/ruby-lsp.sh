#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag --version         Print ruby-lsp version
# @flag --debug           Launch the Ruby LSP with a debugger attached
# @flag --time-index      Measure the time it takes to index the project
# @option --branch        Launch the Ruby LSP using the specified branch rather than the release version
# @flag --doctor          Run troubleshooting steps
# @flag --use-launcher    [EXPERIMENTAL] Use launcher mechanism to handle missing dependencies gracefully
# @flag -h --help         Print this help

command eval "$(argc --argc-eval "$0" "$@")"